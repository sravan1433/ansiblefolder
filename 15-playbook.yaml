- name: loops example
  hosts: localhost
  tasks:
  - name: print the names
    ansible.builtin.debug:
     msg: "hello {{item}}"
    loop:
    - sunny
    - mallesh
    - vittal
    - shiva
    - shashikanth
    